package superadmin_partials

import (
	"context"
	"law_flow_app_go/models"
	"law_flow_app_go/services/i18n"
	"time"
)

templ UsersTable(ctx context.Context, users []models.User) {
	<div class="overflow-x-auto">
		<table class="w-full text-left">
			<thead class="bg-white/5 text-muted-foreground uppercase text-xs font-semibold">
				<tr>
					<th class="px-6 py-4 rounded-tl-xl rounded-bl-xl">{ i18n.T(ctx, "superadmin.users.table.user") }</th>
					<th class="px-6 py-4">{ i18n.T(ctx, "superadmin.users.table.role") }</th>
					<th class="px-6 py-4">{ i18n.T(ctx, "superadmin.users.table.status") }</th>
					<th class="px-6 py-4">{ i18n.T(ctx, "superadmin.users.table.firm") }</th>
					<th class="px-6 py-4">{ i18n.T(ctx, "superadmin.users.table.joined") }</th>
					<th class="px-6 py-4 rounded-tr-xl rounded-br-xl text-right">{ i18n.T(ctx, "superadmin.users.table.actions") }</th>
				</tr>
			</thead>
			<tbody class="divide-y divide-white/5">
				if len(users) == 0 {
					<tr>
						<td colspan="6" class="px-6 py-8 text-center text-muted-foreground">
							{ i18n.T(ctx, "superadmin.users.table.no_results") }
						</td>
					</tr>
				} else {
					for _, user := range users {
						<tr class="hover:bg-white/5 transition-colors group">
							<td class="px-6 py-4">
								<div class="flex items-center gap-3">
									<div class="w-9 h-9 rounded-lg bg-gradient-to-br from-blue-500/20 to-indigo-600/20 flex items-center justify-center font-bold text-blue-400 text-sm">
										{ string(user.Name[0]) }
									</div>
									<div>
										<div class="font-medium text-foreground">{ user.Name }</div>
										<div class="text-xs text-muted-foreground">{ user.Email }</div>
									</div>
								</div>
							</td>
							<td class="px-6 py-4">
								<span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-lg text-xs font-medium bg-white/5 text-muted-foreground border border-white/5">
									{ user.Role }
								</span>
							</td>
							<td class="px-6 py-4">
								if user.IsActive {
									<span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-lg text-xs font-medium bg-green-500/10 text-green-400 border border-green-500/20">
										<span class="w-1.5 h-1.5 rounded-full bg-green-400"></span>
										{ i18n.T(ctx, "superadmin.users.status_filter.active") }
									</span>
								} else {
									<span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-lg text-xs font-medium bg-red-500/10 text-red-400 border border-red-500/20">
										<span class="w-1.5 h-1.5 rounded-full bg-red-400"></span>
										{ i18n.T(ctx, "superadmin.users.status_filter.inactive") }
									</span>
								}
							</td>
							<td class="px-6 py-4">
								if user.Firm != nil {
									<div class="text-sm text-foreground hover:text-accent transition-colors">
										<a href={ templ.SafeURL("/superadmin/firms/" + user.Firm.ID) }>{ user.Firm.Name }</a>
									</div>
								} else {
									<span class="text-xs text-yellow-500/80 italic">{ i18n.T(ctx, "superadmin.users.table.no_firm") }</span>
								}
							</td>
							<td class="px-6 py-4 text-sm text-muted-foreground">
								{ user.CreatedAt.Format("Jan 02, 2006") }
								<div class="text-[10px] opacity-60">{ user.CreatedAt.Format(time.Kitchen) }</div>
							</td>
							<td class="px-6 py-4 text-right relative">
								<div class="flex items-center justify-end gap-2 opacity-0 group-hover:opacity-100 transition-opacity">
									<button
										hx-get={ "/superadmin/users/" + user.ID + "/edit" }
										hx-target="#modal-container"
										hx-swap="innerHTML"
										class="p-2 rounded-lg bg-white/5 text-muted-foreground hover:text-foreground hover:bg-white/10 transition-all border border-transparent hover:border-white/10"
										title={ i18n.T(ctx, "superadmin.users.table.actions") }
									>
										<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path></svg>
									</button>
									<button
										hx-patch={ "/superadmin/users/" + user.ID + "/toggle-active" }
										hx-target="#users-table-container"
										class="p-2 rounded-lg bg-white/5 text-muted-foreground hover:text-foreground hover:bg-white/10 transition-all border border-transparent hover:border-white/10"
										title={ i18n.T(ctx, "superadmin.users.modal.status") }
									>
										<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
									</button>
									<button
										hx-get={ "/superadmin/users/" + user.ID + "/delete-confirm" }
										hx-target="#modal-container"
										hx-swap="innerHTML"
										class="p-2 rounded-lg bg-red-500/5 text-red-400 hover:bg-red-500/10 transition-all border border-transparent hover:border-red-500/20"
										title={ i18n.T(ctx, "superadmin.users.delete_modal.delete") }
									>
										<svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"></path></svg>
									</button>
								</div>
							</td>
						</tr>
					}
				}
			</tbody>
		</table>
	</div>
}
